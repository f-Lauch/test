

net use T: \\ipadresse\freigabe\ordner


sudo apt-get update
sudo apt-get install isc-dhcp-client
sudo dhclient	Dhcp aktivieren

Sudo hostnamectl set-hostname „hostname“

Sudo useradd „benutzername“
Sudo passwd „benutzername“

Sudo usermod -L anton.steiner	

ip a >> linux.txt
free -h >> linux.txt
df -h >> linux.txt


ip nat outside
ip nat inside

access-list 1 permit 192.168.1.0 0.0.0.255
ip nat inside source list 1 interface GigabitEthernet0/0 overload
ip nat inside source static tcp 192.168.1.2 80 10.0.0.254 80
